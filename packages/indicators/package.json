{
  "name": "@neelkanth/indicators",
  "version": "0.0.0",
  "private": true,
  "description": "Pure incremental indicator folds (EMA, RSI, ATR, VWAP, SMA) — one source of truth for the math (plan/18 §2, §7)",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@neelkanth/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  }
}
